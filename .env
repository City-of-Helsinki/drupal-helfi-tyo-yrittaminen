#
# Enviromental values for Makefile and for docker-compose.yml
#

# Docker Compose project name
COMPOSE_PROJECT_NAME=helfi-elo
PROJECT_NAME=tyo-yrittaminen

# Local hostname
DRUPAL_HOSTNAME=helfi-elo.docker.so

# Azure Blob storage location of the database dump used by 'make fresh'
AZURE_DUMP_CONTAINER=hki-database-blob-tyo-yrittaminen-test

# Stage file proxy origin domain
STAGE_FILE_PROXY_ORIGIN=https://stplattaprod.blob.core.windows.net
# Stage file proxy path
STAGE_FILE_PROXY_ORIGIN_DIR=tyoyrittaminen5ce88prod

COMPOSE_PROFILES=

# Public webroot
DRUPAL_WEBROOT=public
